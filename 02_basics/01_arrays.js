//array

const arr = [0,1,2,8,7];

console.log(arr);

const newarr = arr.join()

console.log(typeof newarr);

//splice used to split the orignal array

//slice use to slice the array with out disturbing the orignal array 