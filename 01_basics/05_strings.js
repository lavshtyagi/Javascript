const name = 'lavish'
const repocount = 50

// console.log(name + repocount + "value");

console.log(`hello my name is ${name} and my repo count is ${repocount}`);

const game = new String('hitesff')
console.log(game.length);

console.log(game.charAt(2));

// used to trim space 

// include to find that part 

// replace used to  change substring part into another substring given

